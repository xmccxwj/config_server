server:
  port: 7000
zuul:
  routes:
#    product: /abc/**

  #ignored-patterns:
  #  - /**/product/zuulTest
    abc:
      path: /bbb/**
      serviceId: product
      sensitiveHeaders:

management:
  endpoints:
    web:
      exposure:
        include: "*"
